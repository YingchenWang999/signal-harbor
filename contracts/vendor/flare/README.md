# Flare FDC interfaces

These minimal Solidity interfaces are vendored from
`@flarenetwork/flare-periphery-contracts@0.1.52` under its MIT license. Keeping
only the four interfaces used by Signal Harbor avoids installing that package's
unrelated deployment toolchain in the application dependency graph.
